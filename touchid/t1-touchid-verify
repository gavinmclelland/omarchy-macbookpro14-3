#!/bin/bash
# Retired fail-closed compatibility entry point. Earlier research described
# this name as a future PAM helper, but it never authenticated a fingerprint.
# Keep the path as a hard failure so a stale pam_exec rule cannot mistake a
# transport diagnostic for authentication success.
echo "t1-touchid-verify is retired: no public T1 authentication driver is installed" >&2
exit 2
